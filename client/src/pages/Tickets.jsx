import { Box } from '@mui/material';
import { Header, AllTickets } from '../components/';

const Tickets = () => {
  return (
    <Box mb={5}>
      <Header />
      <AllTickets />
    </Box>
  );
};

export default Tickets;
